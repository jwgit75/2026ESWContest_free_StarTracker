# StarTracker Hardware Acceptance Checklist

아래 항목은 위에서 아래 순서대로 진행합니다. 실패한 단계가 있으면 다음 단계로
넘어가지 않습니다.

## 1. 전원 차단 상태

- [ ] 상단판이 양쪽 기계적 끝점에서 충분히 떨어져 있다.
- [ ] Base IMU가 고정 베이스에 단단히 고정되어 있다.
- [ ] Upper IMU가 ALT 이동 상단판에 같은 축 방향으로 고정되어 있다.
- [ ] Base AD0는 LOW/GND, Upper AD0는 HIGH/3.3 V이다.
- [ ] 두 IMU는 3.3 V 로직을 사용하며 GND가 공통이다.
- [ ] 모터 STEP/DIR 핀이 `config.py`의 BCM 번호와 일치한다.

## 2. 모터 전원 분리 후 I²C

- [ ] 모터 드라이버 전원을 분리한다.
- [ ] `i2cdetect -y 1`에서 `68`, `69`가 모두 보인다.
- [ ] 프로그램의 IMU driver 자동 테스트가 통과한다.
- [ ] 두 판을 같은 방향으로 기울일 때 두 pitch가 같은 부호로 변한다.

## 3. 저속 방향 시험

- [ ] 카메라와 무거운 하중을 제거한다.
- [ ] 비상 전원 차단 수단을 손에 둔다.
- [ ] 상단판을 평행 상태에서 약 1° 벗어나게 한다.
- [ ] 프로그램을 실행하고 240스텝 probe가 ALT축에만 발생하는지 확인한다.
- [ ] AZ축이 움직이지 않는지 확인한다.
- [ ] probe 후 오차 감소 방향으로 상단판이 이동하는지 확인한다.

## 4. 양방향 수렴

- [ ] 상단 pitch가 base보다 약 +2°인 상태에서 ±0.2°로 수렴한다.
- [ ] 상단 pitch가 base보다 약 -2°인 상태에서 ±0.2°로 수렴한다.
- [ ] 완료 로그가 3회 연속 안정 측정을 표시한다.
- [ ] 성공 후 소프트웨어 ALT가 0°로 설정된다.

## 5. 실패 안전성

- [ ] Upper IMU를 분리하면 카메라와 BLE가 시작되지 않는다.
- [ ] 상단판을 손으로 막아 probe 반응이 없으면 보정이 중단된다.
- [ ] 보정 중 Ctrl+C로 종료하면 GPIO와 I²C 자원이 정리된다.
- [ ] BLE 연결을 끊으면 tracking 모터가 중단된다.

## 6. 전체 기능

- [ ] GNSS 위치와 UTC fix를 얻는다.
- [ ] BLE 조이스틱으로 ALT와 AZ를 각각 움직인다.
- [ ] 대각선 입력으로 두 축이 동시에 움직인다.
- [ ] 첫 plate solving이 화면 중심 RA/Dec를 반환한다.
- [ ] tracking 중 1회 drift correction이 성공하고 JSON에 기록된다.
- [ ] 시험 설정 3×10초 시퀀스가 세 파일을 만든다.
- [ ] 촬영 중 버튼을 누르면 현재 프레임 이후 나머지가 취소된다.
- [ ] 최종 설정 18×10초 시퀀스가 완료되고 모든 경로가 로그에 기록된다.

## 7. 결과 판정

- [ ] 정지 촬영과 tracking 촬영을 같은 조건으로 비교했다.
- [ ] 각 프레임 중앙과 외곽의 별 늘어짐을 픽셀 단위로 기록했다.
- [ ] 18개 프레임을 정렬·회전 보정·스태킹해 최종 결과를 확인했다.
- [ ] 시험 날짜, 렌즈, 노출, gain, 날씨, 오차와 실패 내용을 보존했다.
